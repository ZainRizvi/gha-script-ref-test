name: 'PR-Added: Direct + GitHub Scripts (old path)'

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run script at OLD path
        run: bash .github/scripts/direct-regular.sh
