# PR-modified: added by test branch
name: 'Test: Regular + Relative Action + GitHub Scripts'

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run via relative action
        uses: ./.github/actions/relative-regular-github-scripts
