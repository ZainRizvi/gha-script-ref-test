name: 'Branch Action - Reusable - CI Scripts'
description: 'Runs script from ci/ via branch-ref action (reusable workflow)'
runs:
  using: 'composite'
  steps:
    - run: bash ci/moved/action-branch-reusable.sh
      shell: bash
