name: 'Branch Action - Regular - CI Scripts'
description: 'Runs script from ci/ via branch-ref action'
runs:
  using: 'composite'
  steps:
    - run: bash ci/moved/action-branch-regular.sh
      shell: bash
