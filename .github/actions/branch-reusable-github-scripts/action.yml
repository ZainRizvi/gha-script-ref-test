name: 'Branch Action - Reusable - GitHub Scripts'
description: 'Runs script from .github/scripts via branch-ref action (reusable workflow)'
runs:
  using: 'composite'
  steps:
    - run: bash .github/scripts/action-branch-reusable.sh
      shell: bash
