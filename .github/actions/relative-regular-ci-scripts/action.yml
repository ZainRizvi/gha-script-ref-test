name: 'Relative Action - Regular - CI Scripts'
description: 'Runs script from ci/ via relative action ref'
runs:
  using: 'composite'
  steps:
    - run: bash ci/action-relative-regular.sh
      shell: bash
    - name: PR-added step
      run: echo "Step added by PR branch"
      shell: bash
