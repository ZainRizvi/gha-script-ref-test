name: 'Relative Action - Regular - CI Scripts'
description: 'Runs script from ci/ via relative action ref'
runs:
  using: 'composite'
  steps:
    - run: bash ci/moved/action-relative-regular.sh
      shell: bash
