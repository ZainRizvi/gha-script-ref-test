name: 'Branch Action - Regular - GitHub Scripts'
description: 'Runs script from .github/scripts via branch-ref action'
runs:
  using: 'composite'
  steps:
    - run: bash .github/scripts/action-branch-regular.sh
      shell: bash
    - name: PR-added step
      run: echo "Step added by PR branch"
      shell: bash
